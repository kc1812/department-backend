{
  "name": "department",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node ./server/server.js",
    "dev": "set NODE_ENV=development&&node ./server/server.js",
    "prod": "set NODE_ENV=production&&node ./server/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-app": "cd client && npm run build",
    "install-client": "cd client && npm install",
    "heroku-postbuild": "npm run install-client && npm run build-app"
  },
  "author": "Kunal",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^5.9.16"
  },
  "devDependencies": {}
}
